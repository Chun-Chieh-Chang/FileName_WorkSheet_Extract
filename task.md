# 任務執行清單 (task.md)

- `[x]` 在 React 前端 `src/App.jsx` 中，支援 `summaryFiles` 雙向載入（同時加載 2025 與 2026 的數據檔）。
- `[x]` 新增年度切換選擇器 UI 元件，允許用戶切換 `2025`、`2026` 或是啟用 `compare` 跨年度對比。
- `[x]` 在 `compare` 模式下，重構 Chart.js 數據集與座標軸設定，將 2025 與 2026 年度的月度數據並列（不堆疊）呈現，提供直觀的 YoY 對比。
- `[x]` 執行打包編譯 `npm run build`，確保本地測試通過。
- `[x]` 更新 Walkthrough 與開發日誌。
